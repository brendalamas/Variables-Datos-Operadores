/*
Perímetro de un rectángulo

Crear un programa que pida al usuario ingresar el valor 
de la altura y el valor del ancho de un rectángulo, 
calcular su perímetro y mostrarlo en un mensaje.
*/

let altura = prompt ("ingrese el valor de la altura del rectangulo");
let ancho = prompt ("ingrese el valor del ancho del rectangulo");

alert (`El perimetro del rectangulo es ${(altura *2) + (ancho *2)}`);
